#include<stdio.h>
#include<math.h>
int main()
{
    float num,root;
    printf("Enter a number");
    scanf("%f",&num);

    root = sqrt(num);

    printf("Square root is : %f",root);
    return 0;
}
