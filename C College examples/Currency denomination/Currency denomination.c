#include<stdio.h>
int main()
{
    int a[5]={500,100,50,20,10},m,i,temp;

    printf("Enter the Amount\n");
    scanf("%d",&m);
    temp=m;

    for(i=0;i<5;i++)
    {
        printf("\n%d notes is : %d",a[i],temp/a[i]);
        temp=temp%a[i];
    }
}
&item[i].mfg.day,&item[i].mfg.month, &item[i].mfg.year);

&item[i].mfg.day, &item[i].mfg.month, &item[i],mfg.year);
